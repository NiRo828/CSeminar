#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define _CRT_SECURE_NO_WARNINGS
#pragma warning(disable : 4996)
/*
void structmult();
void createList();


int usernum = 0;

struct Mystruct
{
	int val;
	struct Mystruct* next;
};

void createList();
{
	
}

//int mult;
//struct Card
//{
//	int id;
//	int cardnum;
//	int cardop;
//};
//struct Card* curr;
//struct Card* origin;
//
//
void structmult()
//{
//	for (int i = 0; i < mult; i++)
//	{
//		printf("Please Enter a num of mult: ");
//		scanf("%d%d%d", &curr->id, &curr->cardnum, &curr->cardop);
//
//
//	}
//}

void structread()
{
	FILE* f;
	f = fopen("Q1.bin", "r");
	int num = 0;
	int sum = 0;


	for (int = 0; i < 10;i++)
	{
		if (f != NULL)
		{
			fread(&num, sizeof(int), 1, f);
			sum = sum+num;

		}
		else
		{
			printf("error");
			return;
		}
	}
	fclose(f);
	printf("%d", num);
}

int main121212()
{

}
*/