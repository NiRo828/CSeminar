#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define _CRT_SECURE_NO_WARNINGS
#pragma warning(disable : 4996)

/*
struct Linkedlist
{
	int val;
	struct Linkedlist* next;
	
};

struct Linkedlist* head = NULL;
struct Linkedlist* tail = NULL;

void createlist()
{
	int usernum;
	scanf("%d",&usernum);

	struct Linkedlist* curnt;
	struct Linkedlist* prev;
	while (usernum!=100)
	{
		curnt = (struct Linkedlist*)malloc(sizeof(struct Linkedlist));
		curnt->val = usernum;
		if (head == NULL)
		{
			head = curnt;
			tail = curnt;
			curnt->next = NULL;
		}
		else
		{
			head->next = curnt;
			curnt->next = NULL;
			tail = curnt;
		}
		scanf("%d", &usernum);
	}

	

	
}
void printLList()
{
	struct Linkedlist* origin = head;


}

int main2323()
{

}
*/